export interface Group {
    id: string;
    name: string;
    type: string;
    level: number;
}

export interface IdName {
    id: string;
    name: string;
}

export interface IdKey {
    id: string;
    key: string;
}

export interface NextURL {
    next: string;
}
