export interface Group {
    id: string;
    name: string;
    type: string;
    level: number;
}

export interface IdName {
    id: string;
    name: string;
}

export interface NextURL {
    next: string;
}

//export {Group, IdName, NextURL}
